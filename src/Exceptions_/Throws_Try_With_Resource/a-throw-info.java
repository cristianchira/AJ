package Exceptions_.Throws_Try_With_Resource;
 /*
 ---------------------------------------------------throws an exception -----------------------------------------------

        mean specify the exception

        and now we've done that the caller knows the exception can be thrown from this method and because IOException
        is a checked exception the caller then must either catch the exception or also specify that it's going to
         throw it so well actually at that point now we can also remove this part of the try catch block because we're
         not actually dealing with the IOException of such anymore were actually just throwing the error will
         leave the finally block in because we still want to actually process the correct

  */